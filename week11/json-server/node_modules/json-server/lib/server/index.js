"use strict";

const express = require('express');

module.exports = {
  create: () => express().set('json spaces', 2),
  defaults: require('json-server/lib/server/defaults'),
  router: require('json-server/lib/server/router'),
  rewriter: require('json-server/lib/server/rewriter'),
  bodyParser: require('json-server/lib/server/body-parser')
};